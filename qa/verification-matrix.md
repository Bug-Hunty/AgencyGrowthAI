# Verification matrix

| Check | Status | Evidence |
| --- | --- | --- |
| Financial Checkup page implements multi-step state (useState/setStep present) | VERIFIED | [app/financial-checkup/page.tsx](../app/financial-checkup/page.tsx#L22) — const [step, setStep] = useState(1); @ L22; const [form, setForm] = useState<CheckupResponses>({ @ L26; const [contact, setContact] = useState({ @ L39; setStep(2) @ L168; setStep(3) @ L224; setStep(4) @ L84 |
| Financial Checkup preserves data across steps in browser runtime | NOT VERIFIED | [app/financial-checkup/page.tsx](../app/financial-checkup/page.tsx#L22) — const [step, setStep] = useState(1); @ L22; const [form, setForm] = useState<CheckupResponses>({ @ L26; const [contact, setContact] = useState({ @ L39; const handleSubmit = async () => { @ L57; Requires browser execution — Playwright spec pending, CI ONLY |
| Demo repository is the active default | VERIFIED | [lib/repo/index.ts](../lib/repo/index.ts#L49) — export const repo: IRepository = demoRepository; @ L49; export const isDemoMode = !isSupabaseConfigured || true; @ L51 |
| Supabase is gated by env config | VERIFIED | [lib/supabase/client.ts](../lib/supabase/client.ts#L3) — const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL || ''; @ L3; const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || ''; @ L4; export const isSupabaseConfigured = Boolean(supabaseUrl && supabaseAnonKey); @ L6 |
| Agencies RLS exists and is scoped to agency ownership | VERIFIED | [supabase/migrations/20260814031231_create_agencies_and_settings.sql](../supabase/migrations/20260814031231_create_agencies_and_settings.sql#L48) — ALTER TABLE agencies ENABLE ROW LEVEL SECURITY; @ L48; CREATE OR REPLACE FUNCTION user_agency_id() @ L51; CREATE POLICY "select_own_agency" @ L76 |
| Leads RLS exists and allows public insert only with consent | VERIFIED | [supabase/migrations/20260814031317_create_agents_leads_events.sql](../supabase/migrations/20260814031317_create_agents_leads_events.sql#L99) — ALTER TABLE leads ENABLE ROW LEVEL SECURITY; @ L99; CREATE POLICY "public_insert_leads" @ L102; CREATE POLICY "select_own_leads" @ L108 |
| Appointments and candidates RLS exist | VERIFIED | [supabase/migrations/20260814031332_create_appointments_candidates.sql](../supabase/migrations/20260814031332_create_appointments_candidates.sql#L42) — ALTER TABLE appointments ENABLE ROW LEVEL SECURITY; @ L42; ALTER TABLE candidates ENABLE ROW LEVEL SECURITY; @ L97; CREATE POLICY "public_insert_appointments" @ L46 |
| Content and audit RLS exist | VERIFIED | [supabase/migrations/20260814031349_create_content_ai_consents_audit.sql](../supabase/migrations/20260814031349_create_content_ai_consents_audit.sql#L48) — ALTER TABLE content_assets ENABLE ROW LEVEL SECURITY; @ L48; ALTER TABLE audit_logs ENABLE ROW LEVEL SECURITY; @ L168; CREATE POLICY "select_own_content" @ L51 |
